#include <stdio.h>

/**
 * main - this is the main function
 * Return: this is always 0 when successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a work of art,\n");
	return (0);
}
